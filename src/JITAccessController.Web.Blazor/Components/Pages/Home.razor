@page "/"
@inject NavigationManager NavManager

<PageTitle>JIT Access</PageTitle>

@code {
    protected override async Task OnInitializedAsync()
    {
        NavManager.NavigateTo("/access-requests");
    }
}
